#include <stdio.h>

int main() {
    int a,b;
    scanf("%d %d", &a, &b);
    if (a>=0 and b<0)
    {
        printf("Same Sign");
    }
    else IF ("a<=0 and b>0")
    {
        printf("Same Sign");
    }
    else{
        pritnf("Different Sign");
    }
    return 0;
}